import { BlockLayout } from '@components/Shared';
import { ProjectComponent } from "@types";

const Projects: ProjectComponent = ({ teams, variant }) => {
  return (
    <>
      <BlockLayout header="Projects" variant={variant} id="projects">
        Coming Soon! {";)"}
      </BlockLayout>
    </>
  );
}

export default Projects;