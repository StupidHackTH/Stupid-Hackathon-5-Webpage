import { BlockLayout } from '@components/Shared';
import { ProjectComponent } from "@types";

const Projects: ProjectComponent = ({ teams }) => {
  return (
    <>
      <BlockLayout header="Projects" variant={2} id="projects">
        Coming Soon! {";)"}
      </BlockLayout>
    </>
  );
}

export default Projects;